/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  output: 'export',
  pageExtensions: ['tsx'],
  poweredByHeader: false,
  trailingSlash: true,
}

module.exports = nextConfig
