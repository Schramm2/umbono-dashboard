# Umbono evidence shot list

Capture these views only after publication rights and final attribution wording are approved. Use the deterministic local showcase; do not add provider credentials or substitute fabricated live results.

## 1. Evaluation set and run controls

- Viewport: 1440 × 1000.
- Show the “Community guidance review” set, its selected prompt, and three synthetic model profiles.
- Keep the “Deterministic simulation” label visible.
- Capture before running so the visitor can see the complete input state.

## 2. Side-by-side comparison

- Run the selected prompt.
- Capture all response cards with simulated latency, input/output tokens, and illustrative cost visible.
- Keep the notice stating that identities and measurements are synthetic in frame.

## 3. Human-defined weighted scoring

- Select one output and set the sliders to non-default values.
- Keep all criterion labels and weights visible.
- Capture the calculated score and the explanation that the result exists only in browser memory.

## 4. Leaderboard update

- Include the “Session update” marker on the newly scored model.
- Show overall score, values-alignment rate, p95 latency, cost per 1k tokens, token total, and run count.
- Capture the calculation-method note beneath the table.

## Recording option

A 30–45 second recording can follow the same four steps. Do not edit the sequence in a way that implies external model calls, persistent writes, or measured provider performance.
