# Umbono's showcase accepts no environment configuration.
#
# All evaluation data and calculations are deterministic and local. Adding
# credentials here cannot enable external providers, databases, analytics, or
# network-backed behavior.
